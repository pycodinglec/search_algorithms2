# Map metric E2E

Verifies all four algorithms, four maps and both cost modes across recorded trace states. Displayed f/h values round to two decimals; trace values retain full precision. Endpoint markers, accessible labels and entry costs are checked too.
