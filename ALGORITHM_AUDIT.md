# 알고리즘 동작 검증

검증일: 2026-09-15. 알고리즘 네 파일과 시뮬레이터 구현은 변경하지 않았다.

## 검증 기준

- DFS 변형: 후보의 마지막 칸을 꺼내며, 도착 이후에도 후보가 빌 때까지 비용을 개선한다.
- BFS 변형: 후보의 첫 칸을 꺼내며, 도착 이후에도 후보가 빌 때까지 비용을 개선한다.
- 두 변형 모두 더 싼 경로를 발견한 칸을 다시 처리한다. 이미 후보에 있으면 중복 삽입하거나 순서를 옮기지 않는다.
- 최상우선: 직선거리 h가 최소인 후보를 꺼내며, 도착 칸을 꺼내면 종료한다. 최소 비용 보장은 없다.
- A*: g+h가 최소인 후보를 꺼내며, 도착 칸을 꺼내면 종료한다.
- 상하좌우 이동, 같은 크기의 직사각형 지도와 비용 배열, 출발·도착 각 하나, 각 칸 비용 1~9가 검증 범위다. 시작 칸 비용도 합산한다.

## 독립 검증

`tests/test_search_semantics.py`는 네 구현과 별도로 모든 간선을 반복 완화하는 Bellman–Ford 기준값을 계산한다. 고정 사례 5개(한 행, 한 열, 인접한 출발·도착, 막힌 경로 포함), 고정 시드 무작위 지도 300개, 게시 예제 4개 × 비용 모드 2개, 총 313개 지도에 네 알고리즘을 실행했다(1,252건).

실제 실행 중 후보를 꺼낼 때마다 LIFO/FIFO 또는 최소 h/g+h 선택, 후보 중복 유무, 저장된 우선순위와 현재 비용의 일치를 검사했다. DFS·BFS의 모든 도달 가능한 칸의 최종 비용, A*의 도착 비용을 독립 기준값과 비교했다. 네 알고리즘 모두 경로 연결, 벽 회피, 중복 없는 경로, 비용 합계, 도달 불가 처리를 검사했다. DFS·BFS는 후보가 빈 상태로 종료하고, 첫 도착 이후 더 싼 결과로 개선하는 사례가 각각 존재함을 확인했다. 최상우선이 최적 비용보다 비싼 경로를 선택하는 사례도 확인했다.

기존 시험은 32개 게시 실행 기록 전체를 새 CPython 실행 기록 및 독립 파일 실행 출력과 비교한다. 이 검증과 결합해 알고리즘 → 실행 기록 → 웹 표시를 확인한다.

## 공개 사이트 E2E

대상: https://pycodinglec.github.io/search_algorithms2/

2026-09-15 02:33 UTC, 구현 버전 `f0d715ba42c92afaa262102dcaeec36375d2218e`에서 `tools/semantics_e2e.js` 통과:

- 복잡한 지도 DFS: 첫 도착 비용 108 → 계속 실행 → 최종 비용 81, 후보 0개.
- 복잡한 지도 BFS: 첫 도착 후 다음 줄 실행 가능, 최종 비용 81, 후보 0개.
- 비용이 다른 3×3: 최상우선 11, A* 5.

`tools/browser_e2e.js`로 32개 조합의 단계 이동, 코드 강조, 후보·방문 수, 최종 경로·출력, 재생·정지 및 모바일 표시도 검사한다. E2E 실행 결과는 로컬 `output/playwright/audit-*.txt`에 저장한다.

## 결론과 범위

검사한 범위에서 의도와 다른 동작은 발견되지 않았다. DFS·BFS를 방문 한 번 또는 도착 즉시 종료하는 표준 구현으로 해석하지 않는다. 0·음수·두 자리 비용, 비직사각형 입력 등은 이 검증의 보장 범위에 포함하지 않는다. 이는 모든 가능한 입력에 대한 전수검사 결과는 아니다.

재실행: `python3 -B -m unittest discover -s tests -v`
